{
  "name": "ksi-harness-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Hosted demo GUI for ksi-harness. Runs the real tool out-of-process against bundled fixtures — collects evidence with population reconciliation, builds control state, projects the FedRAMP 20x coverage report, and emits schema-valid 20x + OSCAL artifacts. Never contacts a real cloud account.",
  "scripts": {
    "dev": "next dev -p 3220",
    "build": "next build",
    "start": "next start -p 3220",
    "security-gate": "node scripts/security-gate.mjs"
  },
  "dependencies": {
    "ksi-harness": "file:..",
    "next": "16.3.4",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "server-only": "0.0.1",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@types/node": "26.4.1",
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.5",
    "typescript": "7.0.2"
  },
  "overrides": {
    "postcss": "^8.5.28"
  }
}
